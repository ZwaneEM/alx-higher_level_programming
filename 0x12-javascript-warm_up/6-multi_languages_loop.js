#!/usr/bin/node

/* Prints 3 lines using an array of strings in a loop */

const LineArray = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(LineArray[i]);
}
